# UUID-IMG

A little website that allows entering a UUID, (or other 128 bit sequences) and produces an image from the bits.

> Humans do not have the ability to easily check the integrity of a UUID by simply glancing at it. - RFC4122
